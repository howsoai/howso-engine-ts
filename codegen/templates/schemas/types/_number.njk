{%- if schema.enum | enumMatchesType("number") -%}
    {%- for item in schema.enum | sort -%}
        {%- if item is number -%}
{{ item }}{% if not loop.last %} | {% endif %}
        {%- endif -%}
    {%- endfor -%}
{%- else -%}
number
{%- endif -%}